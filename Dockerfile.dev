# Dockerfile para desenvolvimento com hot reload
FROM node:18-alpine

# Definir diretório de trabalho
WORKDIR /app

# Copiar arquivos de dependências
COPY package*.json ./

# Instalar todas as dependências (incluindo devDependencies)
RUN npm install

# Copiar código fonte
COPY . .

# Expor porta 4200 para desenvolvimento
EXPOSE 4200

# Comando para iniciar em modo de desenvolvimento
CMD ["npm", "start"]